import React, {Component} from "react";
import styled from "styled-components";

const Container = styled.div``;

class Encoding extends Component {
    constructor() {
        super();
        this.state = {};
    }

    componentDidMount() {
    }

    render() {
        return <Container>Encoding Component</Container>;
    }
}

export default Encoding;