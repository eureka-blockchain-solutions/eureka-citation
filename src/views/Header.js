import React from "react";
import styled from "styled-components";

const Container = styled.div`
  min-height: 90px;
  background: black;
`;

const Header = () => {
  return <Container>Header Functional Component</Container>;
};

export default Header;
